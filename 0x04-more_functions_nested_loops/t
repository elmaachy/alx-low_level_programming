#include "main.h"

/**
 * _isdigit - check if c is between 0 and 9
 *
 * @c: input c
 *
 * Return: 1 if c its digit else return 0
*/
int _isdigit(int c)
{
	if (c >= 48 && c <= 57)
		return (1);
	else
		return (0);
}
